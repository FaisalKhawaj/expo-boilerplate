// Define the type for translation keys
export type Translations = {
  account: string;
  browse: string;
  sign_up: string;
  login: string;
  toggle_theme: string;
  email:string;
  password:string;
  // Add more keys as needed
};
